# TODO list

- Make an early attempt at what the UI should be: Close to defaults (system font
  from [here](https://css-tricks.com/snippets/css/system-font-stack/) and
  leaning on text and nothing else).
- Go through initial generated code and comment to Claude
- Have a take on the initial pages that need to be in the app and change the
  structure
- Work on some of the features we haven't done yet in Claude.md after trying the
  app for a bit
